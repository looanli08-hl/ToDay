---
phase: 03-timeline-and-recording-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/ToDay/ToDay/Features/Today/ScrollCanvas/EventCardView.swift
  - ios/ToDay/ToDay/Features/Today/TodayScreen.swift
autonomous: true
requirements:
  - TML-01
  - TML-02
  - TML-05

must_haves:
  truths:
    - "EventCardView badge font is 12pt semibold monospaced (not 10pt bold)"
    - "EventCardView duration font is 12pt semibold monospaced (not 13pt bold)"
    - "EventCardView detail line font is 12pt regular tertiary (not 13pt regular)"
    - "EventCardView moodMarker name is 15pt semibold (not 14pt medium)"
    - "EventCardView corner radius is AppRadius.lg / 16pt (not cornerRadius: 14)"
    - "TodayScreen bottom action bar uses .appShadow(.elevated) (no Color.primary shadow)"
    - "TodayScreen ScrollView VStack spacing uses AppSpacing.md (not literal 18)"
    - "TodayScreen scrollCanvasSection VStack spacing uses AppSpacing.sm (not literal 14)"
    - "TodayScreen overviewSection VStack spacing uses AppSpacing.xs (not literal 10)"
    - "TodayScreen signatureSection and summarySection description text is 15pt regular (not 14pt)"
    - "TodayScreen summarySection headline is 15pt semibold (not 16pt semibold)"
    - "Build passes with 180+ tests after changes"
  artifacts:
    - path: "ios/ToDay/ToDay/Features/Today/ScrollCanvas/EventCardView.swift"
      provides: "Event card with compliant typography and corner radius"
    - path: "ios/ToDay/ToDay/Features/Today/TodayScreen.swift"
      provides: "Today screen with compliant spacing tokens and warm shadow"
  key_links:
    - from: "EventCardView.swift"
      to: "AppRadius.lg"
      via: "clipShape + overlay RoundedRectangle"
      pattern: "AppRadius\\.lg"
    - from: "TodayScreen.swift"
      to: ".appShadow(.elevated)"
      via: "bottom action bar modifier"
      pattern: "appShadow\\(\\.elevated\\)"
---

<objective>
Apply UI-SPEC typography, corner radius, spacing, and shadow compliance to EventCardView and TodayScreen.

Purpose: These two files contain the highest-frequency deviations from the design contract. EventCardView is the visual core of the timeline (renders every event). TodayScreen contains the primary layout structure. Getting these right first means every subsequent plan sees clean scaffolding.
Output: EventCardView and TodayScreen fully compliant with 03-UI-SPEC.md typography table, spacing token table, shadow contract, and corner radius table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-timeline-and-recording-polish/03-UI-SPEC.md
@.planning/phases/03-timeline-and-recording-polish/03-RESEARCH.md
@ios/ToDay/ToDay/Features/Today/ScrollCanvas/EventCardView.swift
@ios/ToDay/ToDay/Features/Today/TodayScreen.swift
@ios/ToDay/ToDay/Features/Today/TodayTheme.swift

<interfaces>
<!-- Key tokens — executor uses these directly, no codebase exploration needed -->

AppSpacing tokens (from TodayTheme.swift):
- AppSpacing.xxs = 4pt
- AppSpacing.xs = 8pt
- AppSpacing.sm = 12pt
- AppSpacing.md = 16pt
- AppSpacing.lg = 24pt
- AppSpacing.xl = 32pt
- AppSpacing.xxl = 48pt

AppRadius tokens:
- AppRadius.sm = 8pt
- AppRadius.md = 12pt
- AppRadius.lg = 16pt  ← event cards
- AppRadius.xl = 20pt

Shadow API (AppShadow ViewModifier via .appShadow extension):
- .appShadow(.subtle)   — cards, radius 8, opacity 6%, y 2
- .appShadow(.elevated) — floating elements, radius 16, opacity 10%, y 4
- Shadow color base: Color(red: 0.4, green: 0.3, blue: 0.2)
- FORBIDDEN: .shadow(color: Color.primary.opacity(...))

Typography contract (four sizes, two weights):
- Hero:    .system(size: 33, weight: .regular, design: .serif).italic()
- Heading: .system(size: 23, weight: .regular, design: .serif).italic()
- Body semibold: .system(size: 15, weight: .semibold)
- Body regular:  .system(size: 15)
- Small mono:    .system(size: 12, weight: .regular, design: .monospaced)
- Small mono semibold: .system(size: 12, weight: .semibold, design: .monospaced)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix EventCardView typography and corner radius</name>
  <files>ios/ToDay/ToDay/Features/Today/ScrollCanvas/EventCardView.swift</files>
  <read_first>
    ios/ToDay/ToDay/Features/Today/ScrollCanvas/EventCardView.swift
    ios/ToDay/ToDay/Features/Today/TodayTheme.swift
    .planning/phases/03-timeline-and-recording-polish/03-RESEARCH.md (Typography Gap Analysis section)
  </read_first>
  <action>
Apply all EventCardView deviations from the 03-RESEARCH.md Typography Gap table. Make exactly these changes and nothing else:

1. `kindBadgeTitle` font: change from `10pt bold monospaced` to `.system(size: 12, weight: .semibold, design: .monospaced)`
2. `scrollDurationText` font: change from `13pt bold monospaced` to `.system(size: 12, weight: .semibold, design: .monospaced)`
3. `compactDetailLine` font: change from `13pt regular tertiary` to `.system(size: 12, weight: .regular)` with `.foregroundColor` at tertiary opacity
4. `moodMarker` name text font: change from `14pt medium` to `.system(size: 15, weight: .semibold)`
5. Card corner radius: change `cornerRadius: 14` to `AppRadius.lg` (16pt) — update BOTH the `clipShape(RoundedRectangle(cornerRadius: 14, ...))` call AND the matching `overlay(RoundedRectangle(cornerRadius: 14, ...))` stroke. Use `.continuous` style on both.

Pitfall to avoid: Do NOT change the 4pt color accent bar width or its corner radius (2pt). Do NOT change the event name font (15pt semibold — already correct). Do NOT add `TodayTheme.*` references — use `AppColor.*` and `AppRadius.*` directly.

After editing, grep the file for `TodayTheme.` — remove any new usages if found (existing usages that were already there are acceptable).
  </action>
  <verify>
    <automated>cd /Users/looanli/Projects/ToDay/ios/ToDay && xcodebuild build -scheme ToDay -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "cornerRadius: 14" ios/ToDay/ToDay/Features/Today/ScrollCanvas/EventCardView.swift returns no matches
    - grep "AppRadius.lg" ios/ToDay/ToDay/Features/Today/ScrollCanvas/EventCardView.swift returns 2+ matches (clipShape + overlay)
    - grep "size: 10\|size: 13\|size: 14" ios/ToDay/ToDay/Features/Today/ScrollCanvas/EventCardView.swift returns no matches
    - Build passes (exit 0)
  </acceptance_criteria>
  <done>EventCardView badge is 12pt semibold mono, duration is 12pt semibold mono, detail line is 12pt regular, moodMarker name is 15pt semibold, card corner radius is AppRadius.lg on both clipShape and overlay. Build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix TodayScreen spacing tokens and bottom bar shadow</name>
  <files>ios/ToDay/ToDay/Features/Today/TodayScreen.swift</files>
  <read_first>
    ios/ToDay/ToDay/Features/Today/TodayScreen.swift
    ios/ToDay/ToDay/Features/Today/TodayTheme.swift
    .planning/phases/03-timeline-and-recording-polish/03-RESEARCH.md (Spacing Gap Analysis section + Shadow Violation section)
  </read_first>
  <action>
Apply all TodayScreen deviations from the 03-RESEARCH.md Spacing Gap Analysis and Shadow Violation sections. Make exactly these changes and nothing else:

SPACING (replace literals with tokens):
1. ScrollView VStack spacing literal `18` → `AppSpacing.md` (16pt)
2. `scrollCanvasSection` VStack spacing literal `14` → `AppSpacing.sm` (12pt)
3. `overviewSection` VStack spacing literal `10` → `AppSpacing.xs` (8pt)
4. `bottomActionBar` internal spacing literal `12` → `AppSpacing.sm` (value is same, but replace the literal with the token)

TYPOGRAPHY (fix two description texts and one headline):
5. `signatureSection` description text: change `14pt regular` to `.system(size: 15)` (body regular tier)
6. `scrollCanvasSection` description text: change `14pt regular` to `.system(size: 15)` (body regular tier)
7. `summarySection` headline text: change `16pt semibold` to `.system(size: 15, weight: .semibold)` (body semibold tier)

SHADOW (critical fix):
8. Bottom action bar shadow: find `.shadow(color: Color.primary.opacity(0.06), radius: 18, x: 0, y: 8)` and replace with `.appShadow(.elevated)`

Pitfall: Do NOT change the hero title font ("今日画卷" at 33pt serif italic — already correct). Do NOT change the section titles ("今日脉络", "今日时间轴" at 23pt serif italic — already correct). Do NOT change the bottom action bar corner radius literal `20` — it is locked per UI-SPEC exceptions.
  </action>
  <verify>
    <automated>cd /Users/looanli/Projects/ToDay/ios/ToDay && xcodebuild build -scheme ToDay -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "spacing: 18\b\|spacing: 14\b\|spacing: 10\b" ios/ToDay/ToDay/Features/Today/TodayScreen.swift returns no matches (spacing literals removed)
    - grep "Color.primary.opacity" ios/ToDay/ToDay/Features/Today/TodayScreen.swift returns no matches
    - grep "appShadow(.elevated)" ios/ToDay/ToDay/Features/Today/TodayScreen.swift returns 1+ match
    - grep "size: 16\b" ios/ToDay/ToDay/Features/Today/TodayScreen.swift returns no matches (16pt semibold headline fixed)
    - Build passes (exit 0)
  </acceptance_criteria>
  <done>TodayScreen uses AppSpacing tokens for all four spacing sites, bottom action bar uses .appShadow(.elevated), description texts are 15pt regular, summarySection headline is 15pt semibold. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete, run full test suite:

```bash
cd /Users/looanli/Projects/ToDay/ios/ToDay && xcodebuild test -scheme ToDay -destination 'platform=iOS Simulator,name=iPhone 17 Pro' CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "(Test Suite|PASSED|FAILED|error:)"
```

180+ tests must pass. These are pure visual/token changes — no logic should change so no test failures are expected.
</verification>

<success_criteria>
- EventCardView: badge 12pt semibold mono, duration 12pt semibold mono, detail line 12pt regular, moodMarker 15pt semibold, cornerRadius AppRadius.lg on clipShape + overlay
- TodayScreen: spacing literals replaced with AppSpacing tokens (md/sm/xs/sm), bottom action bar .appShadow(.elevated), description texts 15pt, summarySection headline 15pt semibold
- No new TodayTheme.* references introduced in either file
- Build passes, 180+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-and-recording-polish/03-01-SUMMARY.md`
</output>
