---
phase: 03-timeline-and-recording-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/ToDay/ToDay/Features/History/HistoryScreen.swift
  - ios/ToDay/ToDay/Features/Today/QuickRecordSheet.swift
  - ios/ToDay/ToDay/Features/Today/ScrollCanvas/DayScrollView.swift
autonomous: true
requirements:
  - TML-04
  - TML-05
  - TML-06
  - MAN-01
  - MAN-02
  - MAN-03

must_haves:
  truths:
    - "HistoryScreen date header uses 23pt regular serif (not .title2.bold system shorthand)"
    - "HistoryScreen metricCard values use 23pt semibold rounded (not 26pt bold)"
    - "HistoryScreen insightSection 'life pulse' title uses 15pt semibold (not 18pt bold)"
    - "HistoryScreen metric cards use .appShadow(.subtle) (no inline shadow calls)"
    - "HistoryScreen selectedDayContent VStack spacing uses AppSpacing.md (not literal 20)"
    - "QuickRecordSheet title uses 23pt regular serif italic (not 28pt)"
    - "DayScrollView moodRow minimum height is 44pt (not 38pt)"
    - "DayScrollView start time is 12pt regular monospaced at 60% opacity"
    - "DayScrollView end time is 12pt regular monospaced at 35% opacity"
    - "Build passes with 180+ tests after changes"
  artifacts:
    - path: "ios/ToDay/ToDay/Features/History/HistoryScreen.swift"
      provides: "History screen with compliant typography, spacing, and shadows"
    - path: "ios/ToDay/ToDay/Features/Today/QuickRecordSheet.swift"
      provides: "Quick record sheet with correct heading tier font"
    - path: "ios/ToDay/ToDay/Features/Today/ScrollCanvas/DayScrollView.swift"
      provides: "Timeline scroll canvas with correct timestamp fonts and 44pt mood row"
  key_links:
    - from: "HistoryScreen.swift"
      to: ".appShadow(.subtle)"
      via: "metricCard modifier"
      pattern: "appShadow\\(\\.subtle\\)"
    - from: "DayScrollView.swift"
      to: "minHeight: 44"
      via: "moodRow frame modifier"
      pattern: "minHeight: 44"
---

<objective>
Apply UI-SPEC typography, spacing, and shadow compliance to HistoryScreen, QuickRecordSheet, and DayScrollView timestamp/moodRow elements.

Purpose: HistoryScreen is the history navigation entry (TML-06). QuickRecordSheet is the manual recording entry (MAN-01/MAN-02). DayScrollView's moodRow minHeight and timestamp fonts complete the MAN-03 inline record display and TML-02 timeline typography.
Output: Three files fully compliant with 03-UI-SPEC.md typography, spacing, and shadow contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-timeline-and-recording-polish/03-UI-SPEC.md
@.planning/phases/03-timeline-and-recording-polish/03-RESEARCH.md
@ios/ToDay/ToDay/Features/History/HistoryScreen.swift
@ios/ToDay/ToDay/Features/Today/QuickRecordSheet.swift
@ios/ToDay/ToDay/Features/Today/ScrollCanvas/DayScrollView.swift
@ios/ToDay/ToDay/Features/Today/TodayTheme.swift

<interfaces>
<!-- Key tokens — executor uses these directly -->

AppSpacing tokens:
- AppSpacing.md = 16pt  ← HistoryScreen selectedDayContent spacing

AppRadius tokens:
- AppRadius.md = 12pt  ← metric cards

Shadow API:
- .appShadow(.subtle)   — cards and metric tiles (radius 8, 6%, y 2)
- .appShadow(.elevated) — sheets and floating bars

Typography contract:
- Heading: .system(size: 23, weight: .regular, design: .serif).italic()   ← HistoryScreen date header, QuickRecordSheet title
- Body semibold: .system(size: 15, weight: .semibold)                     ← "生活脉搏" title in insightSection
- Small mono regular: .system(size: 12, weight: .regular, design: .monospaced)  ← timestamps
- Metric value in tiles: .system(size: 23, weight: .semibold, design: .rounded)  ← from UI-SPEC "23pt semibold rounded"

Opacity for timestamp hierarchy:
- Start time: .opacity(0.60)
- End time:   .opacity(0.35)

Touch target:
- moodRow minHeight: 44pt (iOS HIG minimum)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix HistoryScreen typography, spacing, and shadows</name>
  <files>ios/ToDay/ToDay/Features/History/HistoryScreen.swift</files>
  <read_first>
    ios/ToDay/ToDay/Features/History/HistoryScreen.swift
    ios/ToDay/ToDay/Features/Today/TodayTheme.swift
    .planning/phases/03-timeline-and-recording-polish/03-RESEARCH.md (History Screen Assessment section)
  </read_first>
  <action>
Apply all HistoryScreen deviations from the 03-RESEARCH.md History Screen Assessment section. Make exactly these changes:

TYPOGRAPHY:
1. `selectedDayContent` date header: find `.title2.bold()` system shorthand and replace with `.font(.system(size: 23, weight: .regular, design: .serif)).italic()`
2. `insightSection` "生活脉搏" title: change `18pt bold` to `.system(size: 15, weight: .semibold)`
3. `metricCard` metric value: change `26pt bold` to `.system(size: 23, weight: .semibold, design: .rounded)`

SPACING:
4. `selectedDayContent` VStack spacing literal `20` → `AppSpacing.md` (16pt)

SHADOWS:
5. `metricCard`: find any inline `.shadow(color:radius:x:y:)` modifiers and replace with `.appShadow(.subtle)`

Do NOT change:
- `sectionLabel` font (12pt semibold tracking 1.5 — already correct)
- `recordingStatusBar` fonts (already correct per research)
- Date strip cell sizing (40pt × 40pt — locked)
- `recordingStatusBar` green dot color (`Color.green` system semantic — acceptable per research)
  </action>
  <verify>
    <automated>cd /Users/looanli/Projects/ToDay/ios/ToDay && xcodebuild build -scheme ToDay -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "\.title2\.bold\(\)" ios/ToDay/ToDay/Features/History/HistoryScreen.swift returns no matches
    - grep "size: 18\b\|size: 26\b" ios/ToDay/ToDay/Features/History/HistoryScreen.swift returns no matches
    - grep "spacing: 20\b" ios/ToDay/ToDay/Features/History/HistoryScreen.swift returns no matches
    - grep "appShadow(.subtle)" ios/ToDay/ToDay/Features/History/HistoryScreen.swift returns 1+ match
    - Build passes (exit 0)
  </acceptance_criteria>
  <done>HistoryScreen date header is 23pt regular serif italic, insightSection title is 15pt semibold, metric values are 23pt semibold rounded, selectedDayContent spacing uses AppSpacing.md, metric cards use .appShadow(.subtle). Build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix QuickRecordSheet title font and DayScrollView timestamp/moodRow</name>
  <files>
    ios/ToDay/ToDay/Features/Today/QuickRecordSheet.swift
    ios/ToDay/ToDay/Features/Today/ScrollCanvas/DayScrollView.swift
  </files>
  <read_first>
    ios/ToDay/ToDay/Features/Today/QuickRecordSheet.swift
    ios/ToDay/ToDay/Features/Today/ScrollCanvas/DayScrollView.swift
    .planning/phases/03-timeline-and-recording-polish/03-RESEARCH.md (Manual Recording Assessment + Typography Gap table rows for DayScrollView + open question #2)
  </read_first>
  <action>
Three targeted fixes across two files:

FILE 1 — QuickRecordSheet.swift:
1. Sheet title (`sheetTitle`): change font from `28pt regular serif italic` (or however it is currently expressed) to `.system(size: 23, weight: .regular, design: .serif).italic()` — this is the Heading tier

FILE 2 — DayScrollView.swift:
2. `standardEventRow` start time: change from `11pt medium monospaced` to `.system(size: 12, weight: .regular, design: .monospaced).opacity(0.60)` — 12pt regular monospaced at 60% opacity
3. `standardEventRow` end time: change from `10pt medium monospaced` to `.system(size: 12, weight: .regular, design: .monospaced).opacity(0.35)` — 12pt regular monospaced at 35% opacity
4. `moodRow` minimum height: change `minHeight: 38` to `minHeight: 44` — this is the iOS HIG 44pt touch target requirement

Do NOT change:
- QuickRecordSheet mood grid (3-column LazyVGrid with .subheadline.weight(.medium) — acceptable per research)
- QuickRecordSheet outermost VStack spacing (already correct by value; replace literal 24 with AppSpacing.lg if it is a literal)
- DayScrollView `quietGapRow` label font (12pt regular italic — already correct)
- Any font in DayScrollView that is not specifically the start/end time or moodRow minHeight
  </action>
  <verify>
    <automated>cd /Users/looanli/Projects/ToDay/ios/ToDay && xcodebuild build -scheme ToDay -destination 'generic/platform=iOS Simulator' CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "size: 28\b" ios/ToDay/ToDay/Features/Today/QuickRecordSheet.swift returns no matches
    - grep "size: 23.*serif\|serif.*size: 23" ios/ToDay/ToDay/Features/Today/QuickRecordSheet.swift returns 1 match (sheet title)
    - grep "size: 11\b\|size: 10\b" ios/ToDay/ToDay/Features/Today/ScrollCanvas/DayScrollView.swift returns no matches for timestamp-related fonts
    - grep "minHeight: 38" ios/ToDay/ToDay/Features/Today/ScrollCanvas/DayScrollView.swift returns no matches
    - grep "minHeight: 44" ios/ToDay/ToDay/Features/Today/ScrollCanvas/DayScrollView.swift returns 1+ match
    - Build passes (exit 0)
  </acceptance_criteria>
  <done>QuickRecordSheet title is 23pt regular serif italic. DayScrollView start time is 12pt regular mono at 60% opacity, end time is 12pt regular mono at 35% opacity. moodRow minHeight is 44pt. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete, run full test suite:

```bash
cd /Users/looanli/Projects/ToDay/ios/ToDay && xcodebuild test -scheme ToDay -destination 'platform=iOS Simulator,name=iPhone 17 Pro' CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "(Test Suite|PASSED|FAILED|error:)"
```

180+ tests must pass. All changes are visual/token corrections — no logic changes expected.
</verification>

<success_criteria>
- HistoryScreen: date header 23pt serif italic, insightSection title 15pt semibold, metric values 23pt semibold rounded, spacing AppSpacing.md, metric cards .appShadow(.subtle)
- QuickRecordSheet: sheet title 23pt regular serif italic (not 28pt)
- DayScrollView: start time 12pt mono 60% opacity, end time 12pt mono 35% opacity, moodRow minHeight 44pt
- Build passes, 180+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-and-recording-polish/03-02-SUMMARY.md`
</output>
